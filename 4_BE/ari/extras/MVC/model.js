class User {
  constructor(name, email, password) {
    this.name = name;
    this.email = email;
    this.password = password;
  }
}
// This is a simple User model that has a constructor to create a new user with a name, email, and password.
// can be mongoose model aswell
